server:
  port: 9999
  
spring:
  datasource:
    url: jdbc:postgresql://localhost:2021/postgres
    username: postgres
    password: password
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect:  org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: update

