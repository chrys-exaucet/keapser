server:
  port: 9999
  
spring:
  datasource:
    url: jdbc:postgresql://localhost:7887/chat
    username: postgres
    password: root
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect:  org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: update

